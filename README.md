# Compiler for the ___ language
See LanguageSpec.md for an introduction to the ___ language. This compiler uses antlr4 to produce the lexer and parser. The backend is implemented using MLIR/LLVM.
# Usage
## Building
  1. Run `mkdir build; cd build`.
  1. Run `cmake <path-to-compiler-base>`.
  1. Run `make`.
